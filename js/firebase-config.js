// Firebase web config — replace `null` with the config object from
// Firebase console → Project settings → Your apps → Web app, e.g.:
//
// export default {
//   apiKey: '...', authDomain: 'dragon-run-xxxx.firebaseapp.com',
//   projectId: 'dragon-run-xxxx', storageBucket: '...',
//   messagingSenderId: '...', appId: '...',
// };
//
// This config is safe to commit — it identifies the project, it does not grant
// access. Access control lives in Firestore security rules (see README.md).
// Until a config is provided, the app runs in local mode with #s= share links.

export default null;
